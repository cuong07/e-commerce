import React from 'react';
import Stepper from '@/components/stepper/stepper';

const CartCheckout = () => {
    return (
        <div>
            <Stepper step={2} />
        </div>
    );
};

export default CartCheckout;
